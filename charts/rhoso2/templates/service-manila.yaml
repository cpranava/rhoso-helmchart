{{- define "osp.manila" -}}
{{- if .Values.spec.manila.enabled }}
manila:
  enabled: {{ .Values.spec.manila.enabled }}
  template:
    {{- toYaml .Values.spec.manila.apiOverride | nindent 4 }}
{{- end }}
{{- end }}
